var numArray = [30, 32, 40, 55, 60, 80, 90];

// Reverse the array
// var result = numArray.reverse();

// document.write(result);

// push the array
// var result = numArray.push(100);
// document.write(numArray);

// pop the array
var result = numArray.pop();

document.write(numArray);
